apiVersion: v1
kind: Pod
metadata:
  name: evaluate_models
spec:
  containers:
  - name: predict
    image: kubeflow/training-job:v0.2
    command: ["python", "/evaluate_models.py"]
    volumeMounts:
    - name: model
      mountPath: /trained_model
  volumes:
  - name: model
    persistentVolumeClaim:
      claimName: strategy_volume
